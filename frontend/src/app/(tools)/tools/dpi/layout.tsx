import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Free DPI Checker for Print on Demand | Get Mocked',
  description: 'Check image DPI and resolution for print. Calculate if your design is high enough quality. Free, instant.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
