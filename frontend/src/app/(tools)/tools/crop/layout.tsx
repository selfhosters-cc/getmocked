import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Free Image Cropper for E-Commerce | Get Mocked',
  description:
    'Crop images with preset aspect ratios for e-commerce listings and social media. Free, instant, no signup required.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
