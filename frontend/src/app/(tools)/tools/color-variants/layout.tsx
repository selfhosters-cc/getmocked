import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Free Color Variant Generator | Get Mocked',
  description: 'Generate product color variants from a single photo. Create multiple colorways instantly for your e-commerce listings.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
