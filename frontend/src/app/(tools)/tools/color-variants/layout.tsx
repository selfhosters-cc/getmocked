import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Free Colour Variant Generator | Get Mocked',
  description: 'Generate product colour variants from a single photo. Create multiple colourways instantly for your e-commerce listings.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
